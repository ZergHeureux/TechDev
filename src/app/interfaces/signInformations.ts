export interface  SignInformations {
    name?: string,
    schedule?: string,
    isSigned?: boolean,
    signImage?: string,
    student?: {
        firstname?: string,
        lastname?: string,
    }
}